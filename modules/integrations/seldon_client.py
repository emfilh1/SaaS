"""
Заготовка клиента к Seldon.API (агрегатор данных по закупкам)
===================================================================

ВАЖНО: это НЕ рабочий клиент, а размеченный интерфейс-заготовка —
в отличие от eis_client.py/yandexgpt_client.py/gigachat_client.py, где
запрос построен по реальной, публично документированной структуре API
(просто не проверен вживую из-за сетевых ограничений среды разработки).

С Seldon ситуация другая: в открытых источниках (seldon.online,
seldongroup.ru, проверено веб-поиском в августе 2026) есть только
маркетинговое описание того, что умеет Seldon.API — поиск тендеров и
контрактов по ключевым словам/НМЦК/региону, ответ в JSON или XML — но
НЕТ технических деталей: базового URL, способа авторизации, точных
названий методов и параметров запроса. Это нормально — такое обычно
выдают подключённым клиентам сервиса, а не публикуют открыто.

Раз в компании уже пользуются Seldon — правильный следующий шаг: запросить
техническую документацию API у самого Seldon (там же, где оформлялась
подписка, или в поддержке), а не гадать по маркетинговым страницам и
писать код, который выглядит рабочим, но обращается к придуманному
адресу. Ложное "работает" тут хуже честного "ещё не подключено".

Как только документация будет на руках — дописать по образцу
eis_client.request_archive_url(): реальный URL, реальная авторизация,
реальный разбор ответа. Форма данных ниже (SeldonSearchParams,
SeldonTenderResult) уже соответствует тому, что нужно продукту —
скорее всего, менять придётся только реализацию search_tenders(),
не интерфейс.

Источники (проверено веб-поиском, август 2026):
- https://seldon.online/seldon-api/
- https://seldongroup.ru/system/api
- https://seldongroup.ru/system/basis/api
"""

from dataclasses import dataclass, field
from typing import Optional

from modules.core.tender_models import TenderNotice


@dataclass
class SeldonSearchParams:
    """То, что нам нужно спросить у Seldon — форма интерфейса известна из
    открытого описания сервиса (поиск по ключевым словам, НМЦК, региону),
    даже если техническая реализация запроса ещё нет."""
    keywords: list[str] = field(default_factory=list)
    min_nmck: Optional[float] = None
    max_nmck: Optional[float] = None
    region: Optional[str] = None


def search_tenders(api_key: str, params: SeldonSearchParams) -> list[TenderNotice]:
    """Пока не реализовано. Нужна техническая документация Seldon.API
    (базовый URL, формат авторизации, точные названия методов и полей
    ответа) — её нет в открытом доступе, см. докстринг модуля выше.

    Когда документация появится: реализовать HTTP-запрос сюда, разобрать
    ответ и вернуть list[TenderNotice] (модель уже используется Радаром
    и eis_client.py — менять её не нужно, только собрать TenderNotice
    из полей ответа Seldon)."""
    raise NotImplementedError(
        "search_tenders() — заготовка, не реализовано. Нужна техническая "
        "документация Seldon.API (её выдают подключённым клиентам сервиса), "
        "чтобы написать реальный запрос вместо предположений. См. докстринг "
        "модуля seldon_client.py."
    )


if __name__ == "__main__":
    print("seldon_client.py — это заготовка интерфейса, а не рабочий клиент.")
    print("Чтобы подключить по-настоящему, нужна техническая документация Seldon.API")
    print("(базовый URL, авторизация, названия методов) — её нет в открытом доступе,")
    print("нужно запросить у Seldon напрямую как у подключённого клиента сервиса.")

    try:
        search_tenders("fake-key", SeldonSearchParams(keywords=["скобы измерительные"]))
    except NotImplementedError as e:
        print(f"\nОжидаемое поведение — функция честно говорит, что не реализована:\n{e}")

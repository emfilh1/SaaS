# Необязательные тяжёлые зависимости — устанавливать отдельно, только если
# нужна конкретная возможность. Не входят в основной requirements.txt,
# чтобы не тащить их всем, кто просто хочет запустить приложение.
#
# easyocr — OCR-подсказки на вкладке "Чертежи" (модуль drawing_anonymizer.py,
# функция suggest_text_regions_ocr). Тянет PyTorch, при первом запуске
# скачивает модели распознавания (~100 МБ, нужен интернет один раз).
# pip install -r requirements-optional.txt
easyocr
